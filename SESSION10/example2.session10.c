#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int num;
	num=10;
	printf ("hien thi cac so tu 10 den 0:\n");
	while (num >=0)
	{
		printf ("%d\n", num);
		num--;
	}
	
	return 0;
}
